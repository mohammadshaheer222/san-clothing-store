/**
 * Application color constants
 * Using these ensures consistency across the app for both CSS and programmatic usage.
 */
export const COLORS = {
  PRIMARY: '#0C2565',
  SECONDARY: '#FFFFFF',
} as const;
